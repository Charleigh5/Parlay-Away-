import React from 'react';
const HistoryPage: React.FC = () => (
  <div className="p-8">
    <h1 className="text-2xl font-bold">History</h1>
    <p style={{ color: 'hsl(var(--muted-foreground))' }}>Your bet history will be shown here.</p>
  </div>
);
export default HistoryPage;
